# 실제 값은 .env 파일에 작성하고 절대 커밋하지 않는다 (.gitignore에 이미 포함됨)
NODE_ENV=production
PORT=3000

# 클라이언트에서 감지 로그를 업로드할 API 경로
NEXT_PUBLIC_SYNC_ENDPOINT=/api/detections

# ONNX 모델 파일 경로 (public/ 기준 상대경로)
NEXT_PUBLIC_MODEL_PATH=/models/detector.onnx
